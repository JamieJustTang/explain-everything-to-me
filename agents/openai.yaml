interface:
  display_name: "Explain Everything to Me"
  short_description: "按需检索跨 Agent 会话并解释工作进展与决策"
  default_prompt: "Use $explain-everything-to-me to explain what my agents have done recently, grouped by workspace."

policy:
  allow_implicit_invocation: false
